using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class LootDrop : MonoBehaviour
{
    public GameObject explosionSizePOwerupPrefab;
    public GameObject numberOfBombsPowerupPrefab;
    // Start is called before the first frame update
    void Start()
    {

        
    }

    // Update is called once per frame
    void Update()
    {
        
    }



    //void OnDestroy()
    //{
    //    var probabilityOfDrop = 0.25f;
    //    if (Random.Range(0f, 1f) <= probabilityOfDrop)
    //    {
    //        Instantiate(explosionSizePOwerupPrefab, transform.position, Quaternion.identity);
    //    }
    //}
}
